const { colorsMutations } = require("./colors/colors")
const { headerMutations } = require("./header/header")
const { headingMutations } = require("./heading/heading")
const { layoutMutations } = require("./layout/layout")
const { settingsMutations } = require("./settings")
const { spacingMutations } = require("./spacing/spacing")
const { subtitleMutations } = require("./subtitle/subtitle")

module.exports = {subtitleMutations, colorsMutations, headerMutations, headingMutations,layoutMutations, spacingMutations, settingsMutations }